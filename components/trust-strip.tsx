'use client'
import { Check, Sparkles, Users, Zap } from 'lucide-react'
import { useLanguage } from '@/components/language-provider'
export function TrustStrip(){const {t}=useLanguage(); const stats=[["10K+",'trustPhotos',Sparkles],["3×",'trustContent',Zap],["98%",'trustSellers',Users]] as const; return <section className="border-y border-border/70 bg-card/40"><div className="mx-auto grid max-w-6xl gap-px sm:grid-cols-3">{stats.map(([value,label,Icon])=><div key={label} className="flex items-center gap-3 px-4 py-5 sm:justify-center"><span className="flex size-9 items-center justify-center rounded-full bg-primary/10 text-primary"><Icon className="size-4" /></span><div><p className="text-lg font-semibold tracking-tight">{value}</p><p className="text-xs text-muted-foreground">{t(label)}</p></div></div>)}</div></section>}
export function SellerPromise(){const {language}=useLanguage(); return <div className="flex flex-wrap items-center justify-center gap-x-6 gap-y-2 text-xs text-muted-foreground">{(language==='ru'?['Данные защищены','Подходит для Uzum','Можно редактировать']:['Ma’lumotlaringiz xavfsiz','Uzum uchun mos','Tahrirlash mumkin']).map(item=><span key={item} className="inline-flex items-center gap-1.5"><Check className="size-3.5 text-primary" />{item}</span>)}</div>}
