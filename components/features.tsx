'use client'
import { Wand2, Clock, Layers, ShieldCheck } from 'lucide-react'
import { useLanguage } from '@/components/language-provider'
export function Features() { const { t } = useLanguage(); const features = [[Wand2,'smartScene','smartSceneDesc'],[Clock,'seconds','secondsDesc'],[Layers,'variants','variantsDesc'],[ShieldCheck,'readyForUzum','readyForUzumDesc']] as const; return <section id="imkoniyatlar" className="scroll-mt-20 border-t border-border bg-secondary/30"><div className="mx-auto max-w-6xl px-4 py-16 sm:px-6 lg:py-24"><div className="mx-auto mb-12 max-w-2xl text-center"><h2 className="text-balance font-serif text-3xl font-semibold tracking-tight sm:text-4xl">{t('why')}</h2><p className="mt-3 text-pretty leading-relaxed text-muted-foreground">{t('whyDescription')}</p></div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{features.map(([Icon,title,description]) => <div key={title} className="flex flex-col gap-3 rounded-2xl border border-border bg-card p-6"><span className="flex size-10 items-center justify-center rounded-lg bg-primary/15 text-primary"><Icon className="size-5" /></span><h3 className="font-medium">{t(title)}</h3><p className="text-sm leading-relaxed text-muted-foreground">{t(description)}</p></div>)}</div></div></section> }
